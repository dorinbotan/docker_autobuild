version: "3"

services:
  service:
    build: .
    volumes:
      - "test:/root"
  sut:
    build:
      context: .
      dockerfile: Dockerfile.test
    volumes:
      - "test:/test"
    command: [ "/root/test.sh", "0" ]

volumes:
  test:
